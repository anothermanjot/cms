import { FetchContentResponse } from './fetch-content-response';

describe('FetchContentResponse', () => {
  it('should create an instance', () => {
    expect(new FetchContentResponse()).toBeTruthy();
  });
});
